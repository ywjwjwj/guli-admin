import Vue from 'vue';
import { Locale, ComponentLocale, LocalRule } from './type';
export interface Placement {
    [propName: string]: string | number;
}
export interface LocalComponent extends Vue {
    globalLocale: Locale;
}
export default function getLocalReceiverMixins<BasicComponent extends Vue>(componentName: string): import("vue/types/vue").ExtendedVue<LocalComponent & BasicComponent, unknown, {
    t(pattern: LocalRule<Placement>, placement?: Placement): string;
}, {
    locale: ComponentLocale<any>;
}, Record<never, any>>;
